function filtro( dataset25, timeIni, timeFini ){ retorna os que estão no range }

$listafinal = []

$data = requisição('instagram')

while( $data[ultimo][timestamp] > timeFini ){
    paging.
}
else{

    while( $data[ultimo][timestamp] > timeIni ){
        append($listafinal, filtro(data, ......))
        paging;
    }
    else{
        append($listafinal, filtro(data, ......))
        break paging;
    }    
}